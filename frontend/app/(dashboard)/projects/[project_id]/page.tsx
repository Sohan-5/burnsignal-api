// TODO: scaffold with v0
// Project detail — phases, burn timeline, forecast
// Endpoint: GET /api/orgs/{org_id}/projects/{project_id}
export default function ProjectDetail({ params }: { params: { project_id: string } }) {
  return <div>Project {params.project_id} — scaffold with v0</div>
}
